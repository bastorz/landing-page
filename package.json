{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "proxy": "mkcert -install && mkcert localhost && local-ssl-proxy --source 3010 --target 3000 --cert localhost.pem --key localhost-key.pem",
    "devsb": "local-ssl-proxy --source 3010 --target 3000 --cert localhost/localhost.pem --key localhost/localhost-key.pem & nuxt dev"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@types/node": "^18.17.3",
    "autoprefixer": "^10.4.15",
    "local-ssl-proxy": "^2.0.5",
    "nuxt": "^3.6.5",
    "postcss": "^8.4.28",
    "tailwindcss": "^3.3.3"
  },
  "dependencies": {
    "@headlessui/vue": "^1.7.16",
    "@heroicons/vue": "^2.0.18",
    "@nuxtjs/tailwindcss": "^6.8.0",
    "@storyblok/nuxt": "^5.6.5",
    "flowbite": "^1.8.1"
  }
}
